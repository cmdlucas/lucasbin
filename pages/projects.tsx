import React, { FunctionComponent } from 'react';
import { Project } from '../utils/model/projects';

interface ProjectProps {
    projects: Project
}

export const Projects: FunctionComponent<ProjectProps> = (props) => {
    return (
        <>
        
        </>
    )
}

export const getStaticProps = async () => {

}